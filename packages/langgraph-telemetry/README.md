# langgraph-telemetry (Python package)

See the [repository root](../../README.md) for full documentation.

Install:

```bash
pip install langgraph-telemetry
```

Development install:

```bash
pip install -e ".[dev]"
```

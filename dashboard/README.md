# LangGraph Telemetry — dashboard

Next.js app that queries Prometheus **server-side** via `PROMETHEUS_URL` (see `.env.example`).

## Commands

```bash
npm ci
cp .env.example .env.local
npm run dev
```

Production build: `npm run build`

See the [repository root README](../README.md) for the full stack.
